<% if (title) { %>
    <h1><%= title %></h1>
    <% } else { res.redirect('/') } %> 

    <% if (message) { %>
    <p class="notice"><%= message %></p>
    <% } %>
<div class="management_options">    
<a href="../../inv/addClassification" class="addButton">Add a new classification</a>
<a href="../../inv/addVehicle" class="addButton">Add a new vehicle</a>
</div>